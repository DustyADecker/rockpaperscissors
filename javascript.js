let random = Math.random();
let result;
      console.log(random);
      if (Math.random < 0.3) {
        result = "rock";
    } else if (Math.random < 0.6) {
        result = "paper";
    } else 
    return random;
    console.log(result);
    