function getComputerChoice {
    let number = Math.random;
}

/* 
we need the computer to randomly return one of the three
strings "rock", "paper" or "scissors"

create function
select one of the three strings "rock", "paper" or "scissors"
return the selected string

potential method:
create function
initialize variable number
use math.random to generate a random number
code if/else if/else statements so that:
    if number < 0.3 return "rock"
    else if 0.3 < number < 0.6 return "paper"
    else return "scissors"
return string


*/
